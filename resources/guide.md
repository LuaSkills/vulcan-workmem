# Demo Resource Guide

This resource file is intentionally simple.

Its only purpose is to show that the `LuaSkills/demo-skill` repository may also ship plain resources next to runtime scripts and help files.
